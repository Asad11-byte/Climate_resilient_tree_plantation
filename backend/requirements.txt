fastapi==0.115.6
pydantic==2.10.4
pydantic-settings==2.7.1
httpx==0.28.1
qdrant-client==1.19.0
rank-bm25==0.2.2
supabase==2.11.0
python-dotenv==1.0.1
pyjwt==2.10.1

# Phase 2+ (ingestion/RAG) — only the text splitters are actually imported
# (see app/services/ingestion/chunking.py); the full `langchain` package
# was unused dead weight and is the single biggest avoidable contributor
# to serverless function size here.
langchain-text-splitters==0.3.5

# Google Earth Engine
earthengine-api==1.7.42